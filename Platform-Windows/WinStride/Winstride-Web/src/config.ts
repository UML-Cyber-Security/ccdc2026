/** Global WinStride Web configuration. Edit values here to tune behavior. */

/** Maximum events to load per module before stopping pagination. */
export const MAX_EVENTS = 100_000;

/** Number of events fetched per API page. */
export const PAGE_SIZE = 2_000;

/** Polling interval (ms) for new events after initial load completes. */
export const POLL_INTERVAL = 2_000;
