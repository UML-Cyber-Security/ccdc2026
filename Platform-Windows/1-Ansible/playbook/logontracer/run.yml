- name: Schedule extract + upload
  hosts: localhost
  tasks:
    - name: Write cron job
      ansible.builtin.cron:
        name: "Extract + Upload every 30 minutes"
        minute: "*/30"
        job: "{{ playbook_dir }}/run.sh"
