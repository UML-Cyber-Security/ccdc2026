# Auditd Usage: sudo ./auditd.sh
- name: Install auditd
  ansible.builtin.script:
    cmd: /location/of/auditd_script
  become: true

# Sysmon Usage: sudo ./sysmon.sh
- name: Install sysmon
  ansible.builtin.script:
    cmd: /location/of/sysmon_script
  become: true
